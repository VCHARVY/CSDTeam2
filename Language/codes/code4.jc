using LinkedListLibrary;
class Main {
    public static void main() {
        LinkedList list = new LinkedList();
        list.Add(10);
        list.Add(20);
        list.Add(30);

        syswrite("List elements:");
        list.PrintList();

        list.Delete(20);
        syswrite("After deleting 20:");
        list.PrintList();
    }
}
