using SetLibrary;
class Main {
    public static void main() {
        Set<int> set = new Set<int>();
        set.Add(10);
        set.Add(20);
        set.Add(10); // Duplicate

        syswrite("Set size: " + set.Size());
        syswrite("Contains 20? " + set.Contains(20));

        set.Remove(20);
        syswrite("After removing 20, set size: " + set.Size());
    }
}
